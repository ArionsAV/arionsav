/*

NOTES:
	- what should we do if the path to restore already has a file? Maybe copy this file to path + "." + q_id + ".restored" ??
*/

int add_entity_to_quarantine(char *path) {
	return 1;
}

int restore_entity_from_quarantine(long int q_id) {
	return 1;
}
