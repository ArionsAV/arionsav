Just random tools useful for ArionsAV

Currently planning to develop:

- db_append: append data to a database file (ArionsAV_DB)
- exsign: Extract signatures from binary files's .text section and save them in files for later use in db_append 
